# Red Arc Ops Tool — Python dependencies
# Install with:  pip install -r requirements.txt

Flask>=3.0,<4.0
requests>=2.31,<3.0
anthropic>=0.40
python-dotenv>=1.0

# Windows-only: raw dot-matrix printing via the print spooler.
# Skipped automatically on macOS/Linux (dev machines) — the print engine
# falls back to writing the raw bytes to a .prn file there.
pywin32>=306; sys_platform == "win32"

# Build a double-clickable Windows app (optional, for distribution):
pyinstaller>=6.0
